# LT資料：Claude Code の `/code-review` を実際に使ってみた

3〜5分のライトニングトーク一式（エンジニア向け・ターミナルのみで完結）。

| ファイル | 中身 |
|---|---|
| `script.md` | スライド構成と読み上げ原稿。本番はこれを見る |
| `demo-steps.md` | 手元での再現手順とキャプチャ撮影ポイント |
| `review-result.md` | 実際に返ってきた指摘の全文（medium 5件 / high 8件） |

## デモの構造

- `main` … CLIの骨組みと台帳フォーマット、README に会計ルールを明記
- `feature/monthly-summary` … 集計機能を実装した「PR相当」の差分。**バグを仕込んである**

`/code-review` はこの差分をレビューする。実際のPRレビューと同じ流れ。

## 作る順番

1. `demo-steps.md` の通りに手元で実行して、キャプチャを7枚撮る
2. Keynote で `script.md` の構成どおりに10枚作る
3. 通しで読んで4分に収まるか確認する
